#ifndef RANDOM_HPP
#define RANDOM_HPP

namespace kovshikov
{
  template< typename Container >
  void getRandom(Container& contaier, size_t size);
}

template< typename Container >
void kovshikov::getRandom(Container& contaier, size_t size)
{
  for(size_t i = 0; i < size; i++)
  {
    
  }
}

#endif
